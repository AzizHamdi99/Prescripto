const { Appointment } = require('../models');


const addAppointment = async (req, res) => {
    try {
        let info = {
            user_id: req.user_id,
            doctor_id: req.docotr_id,
            date: req.date,
            time: req.time
        }
        const data

    } catch (eroor) {

    }






}
